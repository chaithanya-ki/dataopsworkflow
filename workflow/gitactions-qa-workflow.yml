name: Terraform Deployment - QA

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.0.9

      - name: Deploy Azure Resources - QA
        run: |
          cd terraform/modules
          terraform init
          terraform apply -auto-approve
        env:
          TF_VAR_databricks_host: ${{ secrets.DATABRICKS_HOST_QA }}
          TF_VAR_databricks_token: ${{ secrets.DATABRICKS_TOKEN_QA }}
          TF_VAR_subscription_id: ${{ secrets.SUBSCRIPTION_ID_QA }}
          TF_VAR_resource_group_name: ${{ secrets.RESOURCE_GROUP_NAME_QA }}
